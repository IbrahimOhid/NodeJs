// sum Number
const sumNumber = (a, b)=>{
    return a + b;
}

// multiply Number 
const multiplyNumber = (x, y)=>{
    return x * y;
}

// division Number
const divisionNumber = (d, e)=>{
    return d / e;
}

module.exports = {
    sumNumber, multiplyNumber, divisionNumber,
}


